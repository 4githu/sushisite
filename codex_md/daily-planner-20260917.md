# 일별 계획과 아우라 UX 수정 — 2026-09-17

- 캘린더 `/personal-project/calendar/day`: 왼쪽 일별 메모와 체크리스트, 프로젝트·카테고리별 7일 이내 할 일, 접힌 이후 할 일·아우라 영역. 오른쪽 08:00~다음 날 02:00 시간표. 기존 시작/종료 필드를 사용하며 별도의 마감일 필드는 추가하지 않음.
- 메모는 계정·날짜별 서버 저장. `# 제목`, `- [ ]`/`- [x]` 체크리스트 편집/미리보기. 명시적으로 저장하며 저장되지 않은 변경은 날짜·화면 이동 시 확인. 임의 HTML은 실행하지 않음.
- 기존 카테고리 문자열을 유지하면서 쉼표로 복수 카테고리, `>`로 상하위 경로 지원. 상위 항목 숨김은 하위 경로에도 적용. 여러 분류가 있는 일정은 표시 중인 분류가 하나라도 있으면 유지.
- 아우라는 기존 전용 주간 편집기 복원. 일정 클릭으로 회차 수정 및 학생별 리포트 링크 접근. 제출된 리포트만 완료로 계산. 드래그 등록 및 키보드 선택 지원.
- 공용 캘린더의 리포트 이동은 일반 `webUrl`로 노출. 단일 학생은 리포트, 여러 학생은 학교 화면 연결. URL 검증은 http(s) 및 앱 상대 경로를 허용하고 외부 프로토콜/프로토콜 상대 주소/역슬래시를 거부.
- 요일과 종일 행 고정, 중성 톤 적용. 모바일 일별 화면은 세로 배치.
- 인증은 메모리 캐시 30초와 동시 요청 공유. 서버 인증 만료, 로그아웃 시 무효화. 로그아웃 중 진행된 인증 응답이 계정을 복원하지 못하도록 세대 검사. 다른 탭 로그아웃 시 캐시도 무효화. 네트워크 실패는 로그아웃 화면 대신 재연결 안내. 인증 쿠키의 실제 만료 시간은 변경하지 않음.
- DB는 `calendar_daily_notes` 추가만 필요하며 workspace 초기화가 수행. 실제 사용자 DB에 테스트하지 않음.
- 기존 Re:hear 작업 변경은 그대로 보존. 배포·서비스 재시작은 수행하지 않음.

검증: Svelte check 오류/경고 0, 프로덕션 build 성공, 백엔드 14개, 인증 단위 3개, 브라우저 E2E 7개 통과. 데스크톱/모바일 스크린샷 직접 확인. 프로젝트의 Svelte MCP는 사용 가능한 도구에 없어서 컴파일러 및 브라우저 검사 사용.

미리보기: `/private/tmp/ondo-day-desktop.png`, `/private/tmp/ondo-day-mobile.png`, `/private/tmp/aura-clinic-edit.png` (가상 테스트 데이터).
