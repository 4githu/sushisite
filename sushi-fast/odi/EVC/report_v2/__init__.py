"""Active presentation-report-v2 implementation.

The modules in this package are the only runtime implementation for V2.
Top-level ``odi.EVC.report_*`` modules are compatibility import shims so a
future report package can be selected without rewriting every caller.
"""
